spring.application.name=staffsync_both
# Database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/staffsync
spring.datasource.username=root
spring.datasource.password=Gate@2026
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

spring.datasource.schema=classpath:schema-mysql.sql
spring.session.jdbc.initialize-schema=always

# Logging configuration
logging.level.org.springframework=INFO
logging.level.org.hibernate.SQL=DEBUG

# Server configuration
app.api.key=staffsync_19jul2114sks
